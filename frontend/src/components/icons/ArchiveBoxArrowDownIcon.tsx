
import { ArchiveIcon, LucideProps } from 'lucide-react'; // Changed ArchiveDown to ArchiveIcon

const ArchiveBoxArrowDownIcon = (props: LucideProps) => <ArchiveIcon {...props} />; // Using ArchiveIcon

export default ArchiveBoxArrowDownIcon;
